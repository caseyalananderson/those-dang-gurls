from django.conf.urls import url
from . import views as users_views

urlpatterns = [
    # url(r'^login/$', users_views.user_login, name='user_login'),
    # url(r'^signup/$', users_views.user_signup, name='user_signup'),
]